{# Article preview card component
   Usage: {% include "article-preview.njk" %}
   Expects:
     - previewArticle: { headline, body, word_count, slug, clusters, image_url, imagePrompt }
     - previewEditionDate: edition date for URL
     - previewNewspaperId: newspaper ID for URL
     - previewIsLead: (optional) true for lead article — gets full-width treatment
     - previewTruncate: (optional) word count for excerpt, default 30
#}

{% set truncateCount = previewTruncate if previewTruncate else 30 %}

<div id="{{ previewArticle.slug }}"
     class="article-preview{% if previewIsLead %} article-preview--lead{% endif %}">
  <article>
    {% if previewArticle.image_url %}
      <img src="{{ previewArticle.image_url }}"
           alt="{{ previewArticle.imagePrompt }}"
           loading="{% if previewIsLead %}eager{% else %}lazy{% endif %}"
           width="400"
           height="225"
           class="article-preview__image">
    {% endif %}
    <div class="article-preview__body">
      <h3 class="article-preview__title">
        {{ previewArticle.headline }}
        <a href="#{{ previewArticle.slug }}"
           class="anchor-link article-preview__anchor"
           aria-label="Permalink to: {{ previewArticle.headline }}">#</a>
      </h3>
      <p class="article-preview__excerpt">{{ previewArticle.body | truncateWords(truncateCount) }}</p>
      <p class="article-preview__meta">
        {{ previewArticle.word_count }} words
        <a href="/edition/{{ previewEditionDate }}/{{ previewNewspaperId }}/{{ previewArticle.slug }}/"
           class="article-preview__card-link article-preview__read-more"
           aria-label="Read full article: {{ previewArticle.headline }}">Read article →</a>
      </p>
    </div>
  </article>
</div>
