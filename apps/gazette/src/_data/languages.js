/**
 * 11ty global data file — exposes language configuration to all templates.
 * Returns { system: [...], ui: {...} } from data/languages.json.
 */
export { default } from './lib/loadLanguages.js';
