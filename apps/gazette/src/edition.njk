---
pagination:
  data: editions
  size: 1
  alias: edition
permalink: "edition/{{ edition.date }}/index.html"
layout: layouts/base.njk
ogType: website
eleventyComputed:
  title: "Edition {{ edition.date | dateDisplay }} — Imbryk Gazette"
  description: "Edition {{ edition.date | dateDisplay }} — how advanced AI systems predict this day's events will unfold, seen through six ideological lenses."
  ogImage: "{% if edition.newspapers[0].heroImageUrl %}{{ edition.newspapers[0].heroImageUrl }}{% endif %}"
---

{% set breadcrumbs = [
  {label: "Home", url: "/"},
  {label: edition.date | dateDisplay, url: ""}
] %}
{% include "breadcrumb.njk" %}

<div class="edition-header">
  <h1 class="edition-header__date"><time datetime="{{ edition.date | dateISO }}">{{ edition.date | dateDisplay }}</time></h1>
  {% if edition.curator_synthesis %}
    <a href="/edition/{{ edition.date }}/curator/"
       class="curator-banner"
       style="{{ personas.curator.cssVars }}"
       aria-label="{{ personas.curator.paperName }} — Synthesis &amp; Analysis">
      <span class="curator-banner__label">{{ personas.curator.paperName }}</span>
      <span class="curator-banner__text">{{ (edition.curator_synthesis.text or edition.curator_synthesis.analysis or "") | truncateChars(120) }}</span>
      <span class="curator-banner__cta" aria-hidden="true">&rarr;</span>
    </a>
  {% endif %}
  {% set neighbours = editions | editionNeighbours(edition.date) %}
  {% set editionDatePrev = neighbours.prev %}
  {% set editionDateNext = neighbours.next %}
  {% include "edition-date-nav.njk" %}
</div>

<ul class="front-page-grid" role="list">
  {% for newspaper in edition.newspapers %}
    {% set cardPersona = personas.newspapers | findPersona(newspaper.newspaper_id) %}
    {% set cardNewspaper = newspaper %}
    {% set cardEditionDate = edition.date %}
    {% set cardId = newspaper.newspaper_id %}
    {% include "front-page-card.njk" %}
  {% endfor %}
</ul>
